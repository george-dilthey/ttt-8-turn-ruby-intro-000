#!/usr/bin/env ruby
require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)
counter=0
token = "X"
while counter < 9
  turn(board)
  counter+= 1
  if token=="X"
    token = "O"
  else
    token == "X"
  end
end
